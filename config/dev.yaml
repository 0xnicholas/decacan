# 开发环境配置
profile: dev

logging:
  level: debug
  stdout: true
  file:
    enabled: true
    path: logs/decacan-dev.log
    rotation: daily
    max_files: 3

postgres:
  url: postgres://postgres:postgres@localhost/decacan_dev
  max_connections: 5
  auto_migrate: true
